apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    url: http://prometheus.service.home.consul:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "15s"
