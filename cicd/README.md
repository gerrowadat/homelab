# cicd

CI/CD infrastructure configuration.

| Directory | Contents |
|---|---|
| `web/` | HAProxy upstream config stubs for web services |

The HAProxy configs here define upstream backends that are included into the
main HAProxy config on the reverse proxy host.
